## 1. Implementation

- [x] 1.1 Update `install-cursor` to write project-local `.cursor/mcp.json` and create the `.cursor` directory when needed
- [x] 1.2 Include `"type": "stdio"` in Cursor MCP server config
- [x] 1.3 Preserve existing config; refuse to overwrite unreadable configs unless `--force`
- [x] 1.4 Update docs/specs for Cursor config paths and format
- [x] 1.5 Document Cursor MCP install links and how the config can be used to generate them
- [x] 1.6 Add or update tests for `install-cursor` behavior (project-local and global)
